void write_matrix_value(void* matrix_ptr, 
			int nx, int ny, int nz, int first, 
			int x, int y, int z, double value);
double read_matrix_value(void* matrix_ptr,
			 int nx, int ny, int nz, int first,
			 int x, int y, int z);
